# Tsunami Red Alerts

Standalone coastal-warning dashboard.

- Web UI: `public/`
- Live feed server: `server/`
- Android APK: `android/` plus `Build-Tsunami-APK.bat`
- Start locally with `Start-App.bat` or `npm start`
